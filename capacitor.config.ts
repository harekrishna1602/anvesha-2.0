import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.whistlingwombatswim',
  appName: 'whistling-wombat-swim',
  webDir: 'dist'
};

export default config;
